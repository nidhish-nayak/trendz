{
    "scripts": {
        "pre-commit": "lint-staged",
        "prettier": "prettier --write ."
    },
    "devDependencies": {
        "husky": "^8.0.3",
        "lint-staged": "^15.2.2",
        "prettier": "^3.2.5"
    },
    "lint-staged": {
        "*.{ts,tsx,js,jsx,css,md,json}": "prettier --write",
        "*.json": [
            "prettier --write"
        ]
    }
}
